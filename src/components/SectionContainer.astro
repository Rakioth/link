---
import type { HTMLTag } from "astro/types"

interface Props {
	as?: HTMLTag
	class?: string
}

const { as: Tag = "section", class: className } = Astro.props
---

<Tag class:list={[className, "container mx-auto w-full md:max-w-xl lg:max-w-2xl"]}>
	<slot />
</Tag>
